import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  countContainer: {
    paddingVertical: 12,
    paddingHorizontal: 16,
  },
  countText: {
    fontSize: 14,
    color: '#666',
    textAlign: 'center',
  },
});
